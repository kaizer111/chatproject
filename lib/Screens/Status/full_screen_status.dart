import 'package:flutter/material.dart';

class FullScreenStatus extends StatelessWidget {
  const FullScreenStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
     
    );
  }
}